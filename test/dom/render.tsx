import { render,tag } from "sky-view";

render(<div class="1">
	<div class="2" id="hello">hello world</div>
</div>,document.body);

describe('Set', function () {
	it('id', function () {
		expect(s.size).to.be.equal(3);
	});
});